{
    // Option names, types, default value, and possible values (if, e.g., string; see "enum")
    // https://github.com/neoclide/coc.nvim/blob/master/data/schema.json

    // See VSCode reference for explanation of features and options in coc.nvim
    // https://docs.microsoft.com/en-us/visualstudio/ide/writing-code-in-the-code-and-text-editor?view=vs-2019

    // TODO: Configure code lens for popup revision history (disabled by default)

    //"signature.target": "echo",
    // Disables documentation for completion items
    //"suggest.floatEnable": false,
    // Send diagnostics to the echo area instead of float
    "diagnostic.messageTarget": "echo",
    // Autoformat on save using language-specific autoformatter
    "coc.preferences.formatOnSaveFiletypes": ["python"],
    "suggest.minTriggerInputLength": 1,
    // Does this enable previewing documentation? Doesn't seem to affect it. So what then?
    "suggest.enablePreview": true,
    // Beginning completion with first item selected prevents <CR> from ignoring completion suggestions
    "coc.preferences.noselect": true,
    "coc.preferences.colorSupport": true,
    // Show all diagnostics of current line if not cursuring over a particular diagnostic
    "diagnostic.checkCurrentLine": true,
    // Must be set to false to use the Microsoft PLS for completion rather than JEDI:
    // https://github.com/microsoft/python-language-server
    // The VSCode python extension offers the same option, and python-coc is a port
    // of the VSCode extension.
    // Don't confuse Microsoft PLS with https://github.com/palantir/python-language-server
    // On the difference: https://github.com/microsoft/python-language-server/issues/1284
    // Jedi seems to provide better code introspection. It handles arguments to namedtuple
    // constructors and attributes of namedtuples for example.
    "python.jediEnabled": true,
    // Multiple linters can be enabled
    // https://code.visualstudio.com/docs/python/linting
    // https://github.com/palantir/python-language-server
    // coc-python README's list of options
    "python.linting.pylintEnabled": true,
    // Adds only for completions. Seems to disable popup for function signature.
    "python.autoComplete.addBrackets": false,
    // https://medium.com/3yourmind/auto-formatters-for-python-8925065f9505
    "python.formatting.provider": "black",
    "python.globalModuleInstallation": false,
    "python.analysis.symbolsHierarchyDepthLimit": 10
}

